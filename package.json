{
  "name": "davfive.github.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npx --yes serve dist",
    "publish": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
